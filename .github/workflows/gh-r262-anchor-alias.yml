# This workflow is managed by gh actions-lock.

name: GH-R262 anchor alias parser control

on:
  workflow_dispatch:

env:
  GH_R262_ACTION: &gh_r262_action yas413/gh-r221-owned-action-20260810@main

jobs:
  control:
    runs-on: ubuntu-latest
    steps:
      - name: Direct locked control
        uses: actions/checkout@v7.0.1
      - name: Alias-selected action
        uses: *gh_r262_action
      - name: Harmless completion marker
        run: echo GH_R262_CONTROL_COMPLETE
