# This workflow is managed by gh actions-lock.

name: GH-R262 locked-tag transition control

on:
  workflow_dispatch:

jobs:
  control:
    runs-on: ubuntu-latest
    steps:
      - name: Direct locked control
        uses: actions/checkout@v7.0.1
      - name: Locked external tag
        uses: yas413/gh-r262-lock-provider-20260811@v1
      - name: Harmless completion marker
        run: echo GH_R262_CONTROL_COMPLETE
